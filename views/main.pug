doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, height=device-height, initial-scale=1.0')
    title goboX For MAP
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    #map
    .menu
      .search
        i.icon.icon-mapBox.ir_pm goBox아이콘
        h1.tit
          | goboX 
          strong MAP
        .search-form
          input.text-search(type='text', placeholder='주소를 입력해주세요')
          input.icon-search-black.btn-search(type='button', value='검색')
      // /.search
      .driving
        form#driving(action='/post/driving' method='post' enctype='multipart/form-data')
          .option
            input#option1(type="radio", name="option", value="trafast"  checked="checked")
            label(for="option1") 빠른길
            input#option2(type="radio", name="option", value="tracomfort")
            label(for="option2") 편할길
            input#option3(type="radio", name="option", value="traoptimal")
            label(for="option3") 최적
            input#option4(type="radio", name="option", value="traavoidtoll")
            label(for="option4") 무료우선
          .start
            label(for='start') 출발지
            input#start(type='text', name='start', placeholder='검색후 출발지 버튼을 클릭해주세요', readonly='')
            input#start-x.x(type='hidden', name='start_x')
            input#start-y.y(type='hidden', name='start_y')
          .end
            label(for='start') 도착지
            input#goal(type='text', name='goal', placeholder='검색후 도착지 버튼을 클릭해주세요', readonly='')
            input#goal-x.x(type='hidden', name='goal_x')
            input#goal-y.y(type='hidden', name='goal_y')
          input(type='submit', value='경로 탐색')
      // ./driving
      .result
        .search_result
          .search_info.info
            h3
              em.search_info_hash 오산역
              | 으로 검색하셨습니다.
            p
              | 검색결과 
              em.search_info_cot 0
              | 개의 주소가 검색되었습니다.
          #search_result

        .driving_result
          .driving_info.info
            h2.time 30분
            h3.distance 19km
          #driving_result
          
      // ./result
    script(src='./javascripts/script.js')
    script(src="https://openapi.map.naver.com/openapi/v3/maps.js?ncpClientId=j4pi7zx94n")
    script(src="/javascripts/staticMap.js")
